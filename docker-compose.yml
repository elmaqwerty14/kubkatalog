version: "2"
services:
    www:
        build: .
        ports: 
            - "2023:80"
        volumes:
            - ./www:/var/www/html/
        links:
            - db
        networks:
            - default
    db:
        image: mysql
        ports: 
            - "2022:3306"
        environment:
            MYSQL_DATABASE: untukkatakog
            MYSQL_USER: root
            MYSQL_PASSWORD: 1
            MYSQL_ROOT_PASSWORD: 1
        volumes:
            - ./dump:/docker-entrypoint-initdb.d
            - persistent:/var/lib/mysql
        networks:
            - default
    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        links: 
            - db:db
        ports:
            - 8000:80
        environment:
            MYSQL_USER: root
            MYSQL_PASSWORD: 1
            MYSQL_ROOT_PASSWORD: 1
volumes:
    persistent: